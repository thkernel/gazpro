$("ul.errors").html("");
<% if @production.errors.any? %>
  <% @production.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".productions").html("<%= escape_javascript(render 'index') %>");
  $("#production-modal").modal("hide");
 
<% end %>