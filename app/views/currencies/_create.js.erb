$("ul.errors").html("");
<% if @currency.errors.any? %>
  <% @currency.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".currencies").html("<%= escape_javascript(render 'index') %>");
  $("#currency-modal").modal("hide");
 
<% end %>